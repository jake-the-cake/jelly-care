import { MainCard } from "../components/MainCard"

export const Login = () => {
  return (
    <MainCard
      content={
        <>
          {
            'Login page'
          }
        </>
      }
    />
  )
}